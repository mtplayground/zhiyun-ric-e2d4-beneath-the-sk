export default function App() {
  return (
    <main className="app-shell" aria-labelledby="app-title">
      <section className="boot-panel">
        <p className="eyebrow">Research dashboard shell</p>
        <h1 id="app-title">Beneath the Skin</h1>
        <p>
          React, TypeScript, and Vite are ready for the visualization and
          control modules.
        </p>
      </section>
    </main>
  );
}
